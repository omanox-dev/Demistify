# Example environment variables for Demistify
# Do NOT store real secrets in this file when committing to a public repo.

# FastAPI secret key used to sign JWTs
SECRET_KEY=your-secret-key-here

# Optional: Google Generative AI (Gemini) API key if using GenAI features
GOOGLE_GENAI_API_KEY=

# SQLite database URL (relative path recommended for local dev)
DATABASE_URL=sqlite:///./demistify.db

# Optional: set during production to a real domain or different DB
# DATABASE_URL=postgresql://user:pass@host:5432/demistify
